AUTH="/var/run/lightdm/root/:0"
RFBAUTH="/etc/x11vnc/passwd"
RFBPORT="5900"
LOGFILE="/var/log/x11vnc.log"
IP_ADDRESS="172.16.0."

OPTS="-auth $AUTH -rfbauth $RFBAUTH -rfbport $RFBPORT -o $LOGFILE \
-allow $IP_ADDRESS -display :0 -nocursor -forever -bg -repeat"

#OPTS="-auth /var/run/lightdm/root/:0 -nocursor -rfbauth /etc/x11vnc/passwd \
#-forever -bg -rfbport 5900 -o /var/log/x11vnc.log -allow 172.16.0. \
#-display :0 -repeat"

rc_need="!net xl2tpd"
